# 第 11 章 上下文工程（Context / Compaction / Spill）

## 11.1 上下文预算管理

每个模型有一个上下文窗口（如 200K tokens）。dsh 将消息历史持续追加到 session 日志，随着对话增长，消息历史迟早会超出模型上下文窗口的限制。dsh 的上下文工程就是解决这个问题的三组策略：

- **Context Injection**：在每步请求中注入运行时上下文（时间、当前工作目录、session 引用等）。不压缩，但保持当前事实最新。
- **Compaction**：当上下文压力达到阈值时，压缩（compact）历史——用摘要替换旧消息，或将冗余的工具结果裁剪掉。
- **Spill**：当单一工具返回的内容过大时，将完整内容转储到文件，仅将摘要 + 定位符放入模型视野。

## 11.2 Context Injection

在 `SystemPrompt` 的 `assemble()` 中，`contexts` 数组包含了所有插件的运行时上下文贡献。每步请求都会重新组装，因此上下文总是最新的。

常见的上下文注入插件：

- **time-context**（`packages/context/time-context`）：注入当前时间、时区等。
- **session-reference**（`packages/context/session-reference`）：注入前面轮次的引用信息。
- **agent-instructions**（`packages/context/agent-instructions`）：注入当前 Agent 的配置指令（如工具使用指引）。
- **tmux-context**（`packages/context/tmux-context`）：注入 tmux 会话信息。

上下文注入发生在 `agent/pre-step` 阶段。`time-context` 在 pre-step waterfall 中注入时间信息，`session-reference` 注入 session 的引用摘要。

## 11.3 Compaction

Compaction 是 dsh 中最复杂的子系统之一。它由两个包协作完成：

- **compaction**（`packages/compaction/compaction`）：定义 `CompactionEngine` 抽象基类、`CompactionResult` 类型和 `CompactionTrigger` 类型。
- **compaction-basic**（`packages/compaction/compaction-basic`）：默认的压缩实现。

### 11.3.1 触发时机

Compaction 有两个触发源：

```typescript
export type CompactionTrigger = 'pressure' | 'context-overflow'
```

- **pressure**：`compaction-basic` 在 `agent/pre-step` waterfall 中主动检查 session 事件数量是否超过阈值（由配置中的 `maxEvents` 或 `tokensPerEvent` 估计值决定）。如果超过，触发压缩。
- **context-overflow**：`compaction-basic` 也监听 `agent/request-error`——当 LLM 返回 `context_length_exceeded` 错误时，触发压缩。（注意：这个是 `agent/request-error` waterfall，不是 `llm/stream` waterfall——因为上下文溢出发生在请求发送前，而非流的过程中。）

### 11.3.2 压缩策略

`compaction-basic` 的压缩流程分为两步：

1. **Tool-result pruning**：裁剪较早的 `tool/result` 事件，保留其存在性但不再进入表面。模型看不到这些旧的工具结果。
2. **Summary selection**：选择较早的 `assistant/message` 事件，为其生成摘要（通过调用 LLM 压缩消息内容），然后用摘要消息替换原始消息。

替换操作通过 session 的 `surfaceOp` 实现：被压缩的事件日志不变，但 `SurfaceManager` 的 `replaceGeneration` 增加。`deriveMessages()` 检测到 generation 变化后，清空缓存、重新投影。

### 11.3.3 压缩后的恢复

如果压缩后模型仍然返回上下文溢出（`context_length_exceeded`），`compaction-basic` 在 `agent/request-error` 中再次触发更激进的压缩。第二次压缩可能更激进地裁剪。如果最终仍然无法适配上下文窗口，`agent-loop` 将错误传播到 turn 层面。

### 11.3.4 Compaction 的 session 事件

Compaction 本身也会在 session 日志中留下事件。`compact` 操作会创建一个新的 session 入口点（checkpoint），记录 compact 前后的事件范围、压缩后的消息数量等元数据。这个 checkpoint 在 fork/resume 时使用——一个新的 fork 可以从 compact 后的 checkpoint 开始，而不必重放压缩前的完整事件。

## 11.4 Spill

Spill 是 context engineering 的第三个组成部分，用于处理工具返回结果过大（超过上下文预算）。它由 `packages/spill/spill-policy` 实现。

Spill 的工作机制是：

1. 模型调用一个工具，工具返回大量输出（如 50000 行文件内容）。
2. `spill-policy` 在 `tools/post-execute` 中监听结果。如果结果内容的 token 估计值超过阈值：
   - 将完整结果写入指定的 spill 文件（默认在 `.dsh/spill/` 目录下）。
   - 在 `finalizeContent` 中替换模型可见内容为摘要 + 定位符（如 `结果已溢出至 /path/spill/file.txt`）。
   - 通过 `additionalContexts` 注入一条 user/message——告诉模型额外内容在那里。
3. 下一个 step 中，模型看到 spill 消息，知道有额外文件需要时可以调用 `read_file` 访问它以获取完整内容。

Spill 在 `tools/post-execute` 中拦截结果，并通过 `finalizeContent`（一个同步回调）进行替换。因为 `finalizeContent` 是工具定义的可选钩子，spill-policy 通过取代工具层面的 finalizeContent 实现：

```typescript
// spill-policy 注册对所有工具的最终内容替换
ctx.tools.spillPolicy(threshold)
// 内部实现：在所有注册的工具上注册 finalizeContent 回调
```

## 11.5 三层策略的交互

Context Engineering 的三个组成部分并不是孤立运行的。一个典型的对话生命周期的上下文演进：

1. 早期阶段（前 30 个 step）：无压缩，上下文注入活跃。
2. 中期（30-100 step）：`compaction-basic` 在 `agent/pre-step` 触发 `pressure` 压缩。旧工具结果被裁剪，旧消息摘要化。
3. 高峰期：spill-policy 处理大输出工具。compaction 处理 `context-length-exceeded` 错误。
4. Session 恢复时：从 checkpoint 开始加载压缩后的表面，避免重放压缩前的事件。

三层都在 `agent/pre-step` 或 `agent/request-error` 的 waterfall 上注册，但各自关注不同阶段。

## 思考题

1. `compaction-basic` 的 protocol-pressure 触发条件是基于 token 估计值，但准确的 token 计数在压缩发生时并不精确。如果压缩后的消息仍然超出上下文窗口，如何恢复？
2. Spill 的结果写入文件后，如果在同一个 step 中模型没有立即读取 spill 文件，spill 文件何时被清理？由谁清理？
3. Compaction 修改了 session 表面但保留了原始事件。如果要实现一个"完全删除旧事件"的硬压缩模式，需要修改哪些部分？
4. Context injection 的运行时上下文（如时间和 CWD）在每个 step 中都是最新的，但 session message 的 `deriveMessages()` 缓存了之前的消息。如果注入的上下文变量在同一个 turn 的不同 step 之间发生了变化，模型会看到旧值还是新值？