# 第 10 章 子代理（Subagent / Agent Team）

## 10.1 子代理的动机

当模型需要处理一个独立于主对话的复杂子任务时——比如在一个完全不同的上下文中搜索信息、执行一个长耗时操作或与外部工具交互——启动一个子代理是更干净的方案。子代理拥有自己的 session、自己的事件日志、自己的工具集。主代理不必等待子代理完成，也不必在 prompt 中嵌着子代理的内部状态。

dsh 的子代理由 `packages/subagent/subagent` 中的 `SubagentRuntime` Service 实现，而子代理的消费端由 `packages/subagent/tool-subagent` 提供。

## 10.2 SubagentRuntime 注册表

`SubagentRuntime` 是一个命名注册表。provider 通过 `ctx.subagents.registerProvider()` 注册：

```typescript
// subagent/src/index.ts，第 385-400 行
registerProvider(provider: SubagentProvider): () => void {
  const name = provider.name
  return this.ctx.effect(function* (this: SubagentRuntime) {
    if (this.providers.has(name)) {
      throw new SubagentError(`a subagent provider named "${name}" is already registered`, ...)
    }
    this.providers.set(name, provider)
    yield () => { this.providers.delete(name) }
    this.ctx.emit('subagent/provider-added', provider)
  }.bind(this), 'subagents.registerProvider()')
}
```

这个模式与 `ctx.llm.registerAdapter()` 一致。多个 provider 共存于注册表中，按名称选取。不同的 provider 实现了不同的子代理运行方式：

- **in-process**（`subagent-in-process-driver`）：在同进程中衍生新的 Agent 实例。CoRoutine（协同程序）风格，共享文件系统和资源。
- **fork**（`subagent-fork`）：通过子进程隔离方式衍生。
- **acp**：通过 Agent Communication Protocol 与外部 Agent 通信。

## 10.3 两种运行模式

`SubagentProvider` 暴露两个主要的创建 API：

### 10.3.1 start（一次性运行）

`subagents.start(name, request)` 创建一个一次性运行的子代理。返回 `SubagentRun`——一个由 publisher 持有的句柄，包含运行期间的观察接口。运行结束后，run 的 `result` promise 解决。

```typescript
const run = await ctx.subagents.start('in-process', {
  parent: agent,
  prompt: 'Search for the latest API documentation...',
  capabilities: { outputSchema: true },
})
const result = await run.result
```

一次性运行的子代理在用户侧通常通过 `tool-subagent` 插件暴露为模型可调用的 `subagent` 工具。模型可以调用子代理并等待返回。

### 10.3.2 startContinuable（可延续运行）

`subagents.startContinuable(spec)` 创建一个可延续的子代理，在其自己的 session 中运行。返回 `ContinuableStart`（子 session id 和初始消息 id）。之后可以通过 `subagents.followup()` 向子代理发送更多消息，通过 `subagents.interrupt()` 打断它的当前轮次。子代理可以向父代理报告部分结果：

```typescript
// 子代理内部调用
await ctx.subagents.reportFrom(childAgent, content, options)
```

可延续子代理的 session 是持久化的——跨进程重启后仍然存在，可以通过 session store 恢复。

## 10.4 Capability Negotiation

每个子代理可以声明自己支持哪些能力（`SubagentCapabilities`）：

```typescript
interface SubagentCapabilities {
  outputSchema?: boolean    // 是否支持结构化输出
  depthLimit?: boolean      // 是否支持深度限制
  toolFilter?: boolean      // 是否支持工具过滤
  persona?: boolean         // 是否支持自定义 persona
}
```

`subagents.start()` 会检查请求是否超出了 provider 的能力声明。如果请求 `outputSchema` 但 provider 不支持，抛出 `UNSUPPORTED_CAPABILITY`。

## 10.5 Agent Team

dsh 在 `agent-team` 包中实现了一个多 Agent 协作方案——"Agent Team"。team 的概念是：一个父 Agent 维护多个子 Agent（可延续子代理），父 Agent 不在 prompt 中直接嵌入这些子 Agent 的状态，而是通过 `subagents.followup()` 和他们通信。

"Goal"（目标驱动）子系统进一步扩展了 Agent Team：`packages/goal/goal` 中的 Goal Service 维护一个目标列表，`goal-round-driver` 在每个 turn 开始前检查目标变化，自动将新目标转发给合适的子代理。

goal 驱动的流程：

1. 用户设置一个目标（`改完这个 issue`）。
2. goal driver 在 `agent/pre-step` waterfall 中检查目标列表。
3. 如果检测到新目标，驱动 inject 一条包含该目标的上下文消息。
4. Agent 响应后，goal driver 继续在下一次 pre-step 中检查是否需要重新计划。
5. 当模型判定目标完成，将 `goal/changed` 事件的 `finished` 设置为 true。

## 10.6 深度限制

子代理可以嵌套。为了控制嵌套深度，dsh 实现了 `assertSubagentMaxDepth(maxDepth)`——在 start 时检查请求的 `maxDepth` 是否在允许范围内。默认的 depth 限制由父 Agent 在创建子代理时传递控制参数，`child-agent.ts` 中的 `resolveChildDepth()` 函数负责解析和传递。

## 思考题

1. 一次性运行（one-shot）和可延续（continuable）子代理的事件日志结构有什么不同？可延续子代理的 session 中是否也有 `turn/start` 和 `turn/end`？
2. 子代理的能力协商（capability negotiation）是在运行时检查还是在创建时检查？如果创建一个子代理时没有检查权限，执行环节发现不支持会怎么做？
3. "Goal" 驱动和 Inbox 的 `steer` 机制有什么区别？goal 驱动的注入发生在哪个阶段？
4. Agent Team 中父子代理之间的 session 隔离如何保证？子代理的 session 事件是否出现在父代理的 session 日志中？