# 第 5 章 主循环（Agent Loop：Turn / Step）

## 5.1 Turn 与 Step

Agent 的执行过程可以分解为两个嵌套的循环层次：

- **外层是 Turn**（轮次）：一次用户输入触发一个 turn。turn 的内部可能包含零次或多次 LLM 调用，直到模型产出一个完整回复。
- **内层是 Step**（步骤）：一次 LLM 请求及其触发的工具调用构成一个 step。如果模型请求了工具调用，dsh 会执行工具，将结果作为 `tool/result` 事件写入日志，然后**在当前 turn 内继续下一个 step**——模型看到工具结果后再次请求 LLM，直到模型不再请求工具，turn 结束。

一个 turn 可以有 0 个、1 个或多个 step。一个 step 始终属于一个 turn。这个二分法在 dsh 的事件词汇表中以 `turn/start`、`turn/end`、`step/start`、`step/end` 四个事件明确标记：

```typescript
// packages/core/session/src/types.ts，第 243-256 行
'turn/start': { turn: number }
'turn/end': { turn: number; reason: TurnEndReason }
'step/start': { turn: number; step: number }
'step/end': { turn: number; step: number }
```

每个 turn 有一个递增的编号，每个 step 也有一个编号。`turn/end` 携带一个 `TurnEndReason`，告知结束原因。可能的取值来源自 `agent.ts` 中的 `turn()` 方法里的几种退出路径：

- `{ kind: 'completed' }`——模型产出了最终回复且没有未完成的工具调用。
- `{ kind: 'max-tokens' }`——输出达到了 token 上限，会在下一步输入中推导出剩余部分。
- `{ kind: 'blocked' }`——inbox 没有可供本轮消费的消息。
- `{ kind: 'aborted', reason }`——被取消（用户取消、超时、资源释放）。
- `{ kind: 'error', error }`——LLM 调用或工具执行抛出了不可恢复的错误。

## 5.2 相位机（Phase Machine）

`ReactLoopAgent` 不是一个简单的函数调用循环，它维护一个**相位机**，记录当前 agent 处于什么阶段：

```typescript
// agent.ts，第 38-46 行
type Phase =
  | { kind: 'idle'; lastTurn: number }
  | {
    kind: 'maintenance'
    abort: AbortController
    lastTurn: number
    wakeRequested: boolean
  }
  | {
    kind: 'running'
    abort: AbortController
    turn: number
    step: number
    wakeRequested: boolean
  }
```

三种相位：

- **idle**：Agent 没有正在执行的工作。`lastTurn` 记录最后一个已经完成的 turn 编号。如果 inbox 中有待处理的消息，`wakeDriver()` 会启动一个 running 相位。
- **maintenance**：Agent 正在执行不涉及 LLM 的维护工作（例如 compaction）。由 `runMaintenance()` 方法进入。在此期间，新的用户消息会被 inbox 暂存，但 agent 不会启动 LLM 请求。
- **running**：Agent 正在执行一个 turn 循环。`turn` 字段记录当前 turn 编号，`step` 记录当前 step 编号。`wakeDriver()` 负责从 idle 进入 running。

相位转换通过 `setPhase` 方法提交，该方法会检查状体是否有变化（例如 idle → running），有则触发 `agent/status` 事件：

```typescript
// agent.ts，第 104-111 行
private setPhase(next: Phase): void {
  const previousStatus = this.status
  this.phase = next
  const status = this.status
  if (status !== previousStatus) {
    this.dispatch.emit('agent/status', { status })
  }
}
```

外部观察者通过 `agent/status` 事件获知 Agent 是否处于运行中——`agent/status` 事件是 `agent/request` 和 `llm/stream` 等事件流的外层边界。

## 5.3 turn() 方法：一次完整的轮次

`turn()` 是每个 turn 的执行入口。它在 `kick()` 中被一个 `while (await this.turn()) {}` 循环反复调用。每次调用会推进 `phase.turn` 并执行以下步骤：

```typescript
// agent.ts，第 246-330 行（简化）
private async turn(): Promise<boolean> {
  const turn = phase.turn + 1
  this.session.append('turn/start', { turn })    // (1) 标记 turn 起点
  phase.turn = turn
  let turnEnds: TurnEndReason | null = null
  let target: InboxTarget = 'next-turn'

  while (true) {
    const step = phase.step + 1
    const decision = await this.preStep(target, { turn, step })  // (2) 预演
    if (decision.kind === 'reject') {
      turnEnds = { kind: 'blocked' }
      return false
    }

    this.session.append('step/start', { turn, step })   // (3) 标记 step 起点
    phase.step = step
    for (const message of decision.messages) {
      this.session.append('user/message', message, { surfaceOp: 'append' })  // (4) 写入用户消息
    }
    const stepEnd = await this.step(decision.assembly)   // (5) 执行 step
    if (turnEnds === null || turnEnds.kind !== 'max-tokens') turnEnds = stepEnd

    this.session.append('step/end', { turn, step })      // (6) 标记 step 终点

    if (turnEnds && this.inbox.nextStep.length === 0) {
      await this.dispatch.serial('agent/turn-stopping', { turn, signal })  // (7) turn 即将结束
    }
    if (turnEnds && this.inbox.nextStep.length === 0) break
    target = 'next-step'
  }

  this.session.append('turn/end', { turn, reason: turnEnds })  // (8) 标记 turn 终点

  if (!this.inbox.hasPending) return false
  phase.abort = new AbortController()   // 重置，准备下一轮
  phase.step = 0
  return true   // 告诉 kick() 循环继续
}
```

执行路径的步骤分解：

1. **记录 turn/start**。在开始任何可能失败的操作之前，先把事件写入日志。即使之后抛出异常，`turn/start` 已经持久化——读取日志时能知道该轮次被标记过启动但未完成结束。
2. **preStep 预演**。`preStep` 方法从 inbox 中取出待处理消息，组装 system prompt，然后触发 `agent/pre-step` 的 waterfall 事件——这是插件在模型请求之前做决策的最后机会。插件可以返回 `{ kind: 'reject' }` 拒绝该 step，也可以注入额外的上下文消息。
3. **记录 step/start**。在发起 LLM 请求之前打开 step 边界。
4. **写入 user/message**。将本轮输入的每条消息作为独立事件 append 到 session 日志，并标记 `surfaceOp: 'append'`，使它们进入模型可见表面。
5. **执行 step**。这是核心：向 LLM 发送请求、接收流式响应、解析工具调用。细节在 5.4 节展开。
6. **记录 step/end**。关闭 step 边界。
7. **agent/turn-stopping 事件**。如果 step 已经结束（completed / max-tokens / error）且 inbox 中没有 `next-step` 目标的消息，触发 `agent/turn-stopping` 串行事件——插件（如关闭当前 turn 的 todo 列表）有机会在 turn 关闭前做一些处理。
8. **记录 turn/end**。写入结束原因。如果有 pending 消息，重置相位并返回 `true`，让外层的 `kick()` 继续下一次 turn 迭代。

## 5.4 step() 方法：一次 LLM 请求

`step()` 是 turn 的内层循环，处理一次完整的 LLM 往返——发送请求、接收流式响应、执行工具调用、决定是否继续：

```typescript
// agent.ts，第 332-419 行
private async step(assembly: PromptAssembly): Promise<StepEndReason | null> {
  const system = renderPrompt(assembly)

  while (true) {
    // (1) 构建请求
    const { request, preparedCall } = await this.buildRequest(
      turn, step, assembly.tools, system, this.session.deriveMessages(), signal,
    )
    const assembler = new BlockAssembler()
    const chunkSeqs: number[] = []

    try {
      // (2) 发送流式请求
      const stream = preparedCall?.stream(request)
        ?? this.loopCtx.llm.stream(request)
      for await (const chunk of stream) {
        chunkSeqs.push(this.session.append('assistant/chunk', { turn, step, chunk }).seq)
        assembler.push(chunk)
      }
    } catch (error: unknown) {
      // (3) 错误处理：如果中途被取消但已有部分输出，写入 interrupted assistant message
      // 否则将错误信息作为 llm/stream-error 传播
      throw error
    }

    const finish = assembler.finish

    // (4) 错误/终止时通过 agent/request-error waterfall 让插件决定是否重试
    if (finish.kind === 'error' || finish.kind === 'aborted') {
      const action = await this.dispatch.waterfall(
        'agent/request-error', ..., () => Promise.resolve(undefined),
      )
      if (action?.kind !== 'retry') throw new LlmError(...)
      continue  // 重试
    }

    // (5) 组装 assistant message 并写入日志
    const message = createAssistantMessage(...)
    this.session.append('assistant/message', { turn, step, message, ... }, 
      { surfaceOp: 'append', sourceEventSeqs: chunkSeqs })

    if (finish.kind === 'max-tokens') return { kind: 'max-tokens' }

    // (6) 检查是否有工具调用
    const toolCalls = message.content.filter(block => block.type === 'tool-call')
    if (toolCalls.length === 0) return { kind: 'completed' }

    // (7) 执行工具调用
    const { concluded } = await executeToolCalls(this.loopCtx, turn, step, toolCalls, signal, ...)
    return concluded ? { kind: 'completed' } : null  // null = 未终结，继续下一个 step
  }
}
```

执行路径：

1. **buildRequest**：调用 `this.session.deriveMessages()` 得到当前模型历史，结合 system prompt（system prompt 和工具 schema 在 step 外已组装好，缓存在 `assembly` 对象中）、工具列表，构造请求。如果存在 `agent/request` waterfall，插件可以修改请求配置（route、maxTokens、reasoningEffort）。
2. **流式请求**：通过 `ctx.llm.stream(request)` 发送请求，得到一个异步可迭代对象。每个 SSE 事件通过 `BlockAssembler.push(chunk)` 流式联配成 `Block`（`text`、`tool-call`、`reasoning` 等），同时原始 chunk 被写入 session 日志，以支持 tokens 级别的重放。
3. **错误处理**：如果流式请求抛出异常，检查 `assembler.interruptedBlocks()` 是否有已收到的部分输出——如果有，将其作为 `interrupted: true` 的 `assistant/message` 写入日志（保证模型可见了这部分内容）。如果请求前被取消且没有任何输出，则不写入 assistant 消息。
4. **agent/request-error**：`assembler.finish` 报告流是否以 error 或 aborted 结束。如果是，触发 `agent/request-error` waterfall——插件可以返回 `{ kind: 'retry' }` 使整个 step 从头再来。这个机制处理了 LLM 服务端的偶发错误（如上一章讨论的 `<error_finish>` 在流中间报错的情形）。
5. **组装 assistant message**：`assembler.blocks()` 给出所有已经组装的 Block 数组，连同 sourceEventSeqs（引用产生该消息的 chunk 事件序号），写入 session 日志。
6. **检查工具调用**：解析消息中的 `tool-call` 类型块。如果没有，turn 以 completed 结束。
7. **执行工具调用**：`executeToolCalls` 按 `maxParallelToolCalls` 并发调度工具执行。每个工具的结果写入 `tool/result` 事件。如果所有工具都成功执行（`concluded` = true），`step()` 返回 `null`——注意，返回 `null` 给 `turn()` 意味着**本 step 未终结**,上层循环会再调一次 `preStep` 和 `step`。`null` 与 `{ kind: 'completed' }` 的区别在于：completed 表示模型没有调工具，消息直接结束；null 表示模型调了工具、工具执行完毕、结果已经以 tool/result 事件写入日志，等待下一个 step 把工具结果发给模型。

## 5.5 Inbox

inbox 是连接外部消息与 Agent 主循环的队列。它维护两个目标队列：

```typescript
// agent.ts 第 87-91 行的初始化
this.inbox = new Inbox(session, {
  inserted: (message) => { ... },
  discarded: (message) => { ... },
  claimed: (message, turn) => { ... },
})
```

`InboxTarget` 有两个取值：`'next-turn'`（下一轮用户输入）和 `'next-step'`（本轮内额外输入）。`ReactLoopAgent` 暴露四个方法操作 inbox：

- **send(message, target, wakeup)**：将消息插入指定目标队列，可选的唤醒参数会调用 `wakeDriver()` 启动 agent。
- **followup(message)**：等价于 `send(message, 'next-turn', true)`——发给下一轮。
- **steer(message)**：等价于 `send(message, 'next-step', true)`——发给当前 step 之后的下一个 step（例如工具执行后模型请求之前）。
- **inject(message)**：等价于 `send(message, 'next-step', false)`——注入上下文但不唤醒。

`preStep` 方法在 turn 迭代的第一步从 inbox 取出消息：

```typescript
// agent.ts，第 225-243 行
private async preStep(target: InboxTarget, position: { turn: number; step: number }) {
  const claimed = this.inbox.claim(target, position.turn)
  // ...系统提示组装、agent/pre-step waterfall
}
```

`target` 决定 inbox 从哪个队列取消息。第一个 step 的 `target = 'next-turn'`，取出用户新输入的消息。后续 step 的 `target = 'next-step'`，取出当前 turn 内注入的额外消息（工具执行注入的上下文、steer 消息等）。

## 5.6 事件时序总览

结合 `turn()` 和 `step()` 的代码，一个典型的 turn（用户输入 → 模型回复 → 工具调用 → 模型回复）在事件日志中留下的时序为：

```
turn/start                    ← turn 边界打开
  user/message                ← 用户输入（surfaceOp: append）
  step/start                  ← step 1 打开
    request/header            ← 请求配置快照
    assistant/chunk × N      ← 流式 token 级别事件
    assistant/message         ← 模型输出（surfaceOp: append）
    tool/call                 ← 模型请求的工具调用（原文 JSON）
    tool/result               ← 工具执行结果（surfaceOp: append）
  step/end                    ← step 1 关闭
  step/start                  ← step 2 打开
    request/header            ← 请求配置快照（只有变化时）
    assistant/chunk × N
    assistant/message         ← 模型最终回复（surfaceOp: append）
  step/end                    ← step 2 关闭
turn/end                      ← turn 边界关闭
```

每个 step 的 `assistant/message` 记录其引用的 chunk 事件序号（`sourceEventSeqs`），使得模型输出与原始 token 序列之间有一条可回溯的引用链。`tool/call` 和 `tool/result` 用 `callId` 配对。

事件写入的时机值得注意：`turn/start` 在一切处理开始之前写入，`turn/end` 在一切处理结束之后写入。如果进程在 turn 中间崩溃，日志以 `turn/start` 开头但没有对应的 `turn/end`——重放时可以通过这个不对称的边界判断该 turn 未被正常关闭。类似地，`step/start` 没有匹配的 `step/end` 也是一个未完成的 step。

## 5.7 从 while 循环到事件瀑布

回到全书开篇的定位：Claude Code 是一个 while 循环及其护栏。dsh 的主循环从代码结构上看也确实是一个 while 循环——`kick()` 中的 `while (await this.turn()) {}` 和 `turn()` 中的 `while (true) { ... step() ... }`。但差异在于这个循环的每个边界点都开放给插件参与：

- `agent/pre-step`：接受还是拒绝这个 step，注入额外的上下文消息。
- `agent/request`：修改 LLM 请求的配置（换一个 provider 和 model）。
- `agent/request-error`：流式响应出现 error 或 aborted 时决定是否重试。
- `agent/turn-stopping`：turn 即将结束时做最后处理。
- `agent/status`：相位变化时通知外部。
- `agent/error`：未捕获的错误传播路径。

这些事件全部是 Cordis 的事件域（typed event）而不是回调注入。插件不需要继承 Agent 基类或实现某个接口；它只需要在 `apply(ctx)` 中用 `ctx.on('agent/pre-step', ...)` 注册监听器。多个插件可以同时监听同一个事件，waterfall 事件要求每个监听器调用 `next()` 才能继续——这就是**没有特权内核**的含义：agent 只是一个事件驱动的执行器，每个边界决定都是由多个插件通过事件轮流参与达成的。

## 思考题

1. `turn()` 的 while 循环在什么条件下退出？`kick()` 作为外层调用者，如何决定是否开启下一个 turn？
2. `step()` 返回 `null` 与返回 `{ kind: 'completed' }` 的区别是什么？模型调用工具和执行工具分别对应哪种返回？
3. `agent/pre-step` 事件是一个 waterfall 事件。如果两个插件都注册了该事件，一个返回 `reject` 一个返回 `enter`，最终结果由谁的返回值决定？
4. 日志中的 `turn/start` 事件在 `session.append()` 执行后多久才可能有对应的 `turn/end`？如果进程在 turn 中间崩溃，重放时如何识别未完成的 turn？