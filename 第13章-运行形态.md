# 第 13 章 运行形态（Web UI / Headless / Python SDK）

## 13.1 三种运行形态

dsh 的架构支持三种主要的运行形态，而不需要修改核心代码。它们是同一个组合树的不同加载配置：

- **Web UI 形态**：加载 Web 服务器插件，在浏览器中展示交互式界面。
- **Headless 形态**：无 UI，在命令行中接受输入、输出文本。
- **Python SDK 形态**：通过 Python 包与其他应用集成。

## 13.2 Web UI

dsh 内置一个 Web 服务器，默认监听 `localhost:3914`。它由以下插件组成：

- **webserver**：Express 风格的 HTTP 服务器（可能是 Bun 的内置 serve 或 Koa），提供 REST API 和 WebSocket 支持。
- **apiproxy**：将 WebSocket 连接映射到 Agent 的 inbox，实现双向转发。
- **server** 命名空间：提供 `GET /session/:id`、`POST /session/:id/message`、`GET /inspect` 等端点。

Web UI 的前端是一个 SPA（单页面应用），通过 WebSocket 接收 `agent/status`、`agent/inbox/inserted`、`session/event` 等实时事件。tsukasa 是 UI 框架（dsh 的内建 UI 库）。

UI 通过这些 API 与 dsh 通信：

```
WS /api/ws/session/:id    ← 双向事件流（用户输入 → agent/status → session/event）
GET /api/inspect          ← 组合树查看
POST /api/session/:id/message  ← 发送用户消息
POST /api/session/:id/cancel   ← 取消当前 turn
POST /api/session/:id/fork     ← 创建分支
```

Web UI 的审批流程（`approval/request`）通过 apiproxy 转发到浏览器。用户在 UI 弹窗中批准或拒绝，决策通过 WebSocket 返回。

## 13.3 Headless

Headless 形态用于自动化脚本、CI/CD 场景。它通过 `commands` 插件（`packages/interaction/commands`）在命令行中工作。核心交互：

```bash
dsh start --config headless.yml
# 然后通过命令发送消息
dsh tell "帮我写一个 Python 脚本"
```

headless 形态中的审批机制：当工具需要审批时，`approval/request` 监听器通过 `acp`（Agent Communication Protocol）将请求转发到配置好的代理端点，或者直接拒绝（当没有监听器响应时，policy 默认拒绝）。

## 13.4 Python SDK

Python SDK 不是 dsh 仓库的一部分，而是通过 `claude-agent-sdk-python` 包提供。SDK 通过 WebSocket 或 REST API 与 dsh 实例通信。

SDK 中的 `Client` 类：

```python
from claude_agent_sdk import Client

client = Client(
    base_url="http://localhost:3914",
    api_key="...",
)

# 创建一个会话
session = client.create_session()

# 发送消息
for event in session.stream("Hello"):
    print(event.text)
```

SDK 的流式响应使用了 SSE（Server-Sent Events）。dsh 的 Web 服务器在 `/api/session/:id/message` 端点中，当 `Accept: text/event-stream` 头存在时返回 SSE 流，否则返回 JSON。

## 13.5 Session 的隔离与共享

所有三种形态下，session 隔离遵循相同的规则：每个 session 有自己的事件日志、自己的 SurfaceManager、自己的 Agent（如果存在）。一个 dsh 实例可以同时运行多个 session，每个 session 有独立的 Agent。

Web UI 中用户看到的是当前 session 的消息列表。如果另一个 session 在后台运行，UI 不会自动切换。

多个 session 共享同一个 Cordis App、同一组插件实例。这意味着工具的注册、System Prompt 的配置修改、Compaction 策略的变化对所有 session 同时生效。这与"每个 session 有自己的 agent-scoped Context"的隔离规则一致——工具注册在 App 层面，工具可见性在 scope 层面。

## 13.6 进程模型

所有三种形态目前都运行在同一个 Node.js/Bun 进程中（in-process 模式）。dsh 不强制多进程隔离。子代理的 in-process provider 直接在主进程中创建子 Agent。这意味着：

- 主进程崩溃会导致所有 session 丢失（尚未 flush 的数据）。
- 内存使用是所有 session 的叠加。
- 没有进程间隔离的"硬"沙箱。

fork 类型的子代理 provider 尝试通过子进程解决隔离问题，但该 provider 是可选的，且 fork 有额外的序列化开销。

## 思考题

1. Web UI 使用 WebSocket 方式 vs Python SDK 使用 SSE 方式接收事件，这两种方案各自的优劣是什么？
2. Headless 形态中的审批（approval/request）如果没有监听器响应，最终行为是什么？`tools/pre-execute` 返回 `ask` 但无人回答，工具是否会被阻止？
3. 同一个 dsh 实例中的多个 session 共享系统提示词的 section 注册。如果 session A 通过代理增删了一个 section，session B 在下一次请求中会看到这个变化吗？
4. dsh 当前是单进程模型。如果要支持多进程部署——每个用户一个独立进程——需要修改哪些架构层？存储是否有向共享存储（如 PostgreSQL）迁移的能力？