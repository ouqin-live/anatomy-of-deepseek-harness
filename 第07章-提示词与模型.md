# 第 7 章 提示词与模型（System Prompt / LLM Adapter）

## 7.1 System Prompt 的组装

System Prompt 在 dsh 中不是一段静态文本。它是一个由多个 Section 组成的结构化产物。组装由 `SystemPrompt` 这个 Cordis Service 完成。

### 7.1.1 Section 的声明

每个插件可以通过 `ctx.systemPrompt.section()` 注册一个 Section：

```typescript
// system-prompt/src/index.ts，第 381-389 行
section(section: PromptSection): () => void {
  return this.layers.effect(
    this.ctx,
    layer => layer.sections.insert(section.name, section),
    { label: 'systemPrompt.section()' },
  )
}
```

Section 包含三个要素：

```typescript
export interface PromptSection {
  readonly name: string       // 唯一名称
  readonly order: number      // 拼接顺序
  readonly text: string | ((context: AssembleContext) => string)
  readonly complete?: boolean // 独占标志
}
```

- **name**：必须是唯一的。同名 section 在同一个 layer 中被第二个注册抛出错误。这是为了跨 scope 的阴影替换而设计的——scoped section 和全局 section 同名时，scope 内的组装使用 scoped 版本。
- **order**：决定 Section 在最终 prompt 中的位置。约定俗成的排序：`-100` 是 harness 身份声明，`0` 是部署 persona，`100`–`199` 是工具指引。
- **text**：静态字符串或动态函数。动态函数在每次组装时被调用，返回当前内容。
- **complete**：标记该 section 是完整的 system prompt。如果有两个标记为 complete 的 section 同时激活，组装抛错。

### 7.1.2 组装流程

`SystemPrompt.assemble()` 方法的执行路径：

```typescript
// system-prompt/src/index.ts，第 467-542 行
async assemble(context: AssembleContext = {}): Promise<PromptAssembly> {
  // 1. 收集变量（全局 → scope 链，最近的 scope 覆盖最远的）
  // 2. 合并 section（全局 base + scope override）
  // 3. 租约工具 provider，收集所有可见工具 schema
  // 4. 按 order 排序 section
  // 5. 按 toolOrder 排序工具 schema
  // 6. 触发 system-prompt/assemble waterfall
  // 7. 如果指定了 complete section，恢复为唯一的 section
}
```

组装结果是一个 `PromptAssembly`：

```typescript
export interface PromptAssembly {
  sections: AssembledSection[]     // 排好序的提示词 section
  contexts: AssembledContext[]     // 动态运行时上下文
  tools: ToolSchema[]              // 模型可见的工具定义
  variables: Record<string, string | undefined>  // {{变量}}
}
```

`renderPrompt(assembly)` 将这个 assembly 转为最终的字符串：将所有 section 的文本进行 `{{variable}}` 插值后，用空行连接。`renderContextSnapshot(assembly)` 做同样的事但产出的上下文部分。

### 7.1.3 Variable 系统

Prompt 中可以使用 `{{variable_name}}` 语法引用变量。变量由插件通过 `ctx.systemPrompt.variable(name, provider)` 注册：

```typescript
ctx.systemPrompt.variable('date', () => new Date().toISOString().slice(0, 10))
ctx.systemPrompt.variable('cwd', () => process.cwd())
```

变量的求值是**懒的**：provider 函数在每次 `assemble()` 时被调用一次。如果一个 Section 引用了 `{{date}}`，该变量在所有引用它的 Section 中保持一致的值（同一个 assembly 内）。

变量的名称必须匹配 `[a-z][a-z0-9_]*`。未注册的变量引用在渲染时抛错——这是 strict variable policy：不会出现未定义变量时静默替换为空字符串的情况。

### 7.1.4 Runtime Context

除了一般的 Section，SystemPrompt 还有一个独立的 `context` 注册通道：

```typescript
ctx.systemPrompt.context({
  name: 'time-context',
  order: 0,
  text: () => `Current time: ${new Date().toLocaleString()}`,
})
```

context 和 section 的区别：section 出现在 system prompt 前面（作为完整的系统提示词），而 context 出现在消息列表的**动态快照**中。在 `renderContextSnapshot()` 中，它们被包装为：

```
Current runtime context. This snapshot supersedes earlier runtime-context snapshots.

{每个 context 的文本用空行连接}
```

这个快照作为一条 `user/message` 前缀插入到模型请求中。它不是 system prompt 的一部分，而是作为最新的事实状态注入到消息历史中。

## 7.2 LLM Adapter

模型调用由 `LlmRuntime` Service 管理。它维护一个 adapter 注册表：

```typescript
// llm/src/index.ts，第 99-107 行
registerAdapter(routes: string[], adapter: LlmAdapter): AdapterRegistrationHandle
```

每个 adapter 是一个实现了 `LlmAdapter` 接口的 Cordis 插件。接口中两个核心方法：

```typescript
interface LlmAdapter {
  /** 返回该 adapter 支持的 provider 列表 */
  providerInfo(provider: string): LlmProviderInfo
  /** 发起一次流式模型调用 */
  stream(request: LlmCallConfig): AsyncIterable<StreamChunk>
}
```

`stream()` 接收 `LlmCallConfig`——一个包含 `model`、`messages`、`tools`、`system`、`maxTokens` 等字段的完整请求。返回 AsyncIterable，产出 `StreamChunk`（`text`/`reasoning`/`tool-call-begin`/`tool-call-end`/`usage` 等）。

### 7.2.1 Adapter 路由

一个 adapter 可以注册多个 provider（路由名）：

```typescript
// llama.cpp adapter→provider: llama-cpp
// pi-ai adapter→provider: opus, sonnet, claude-code (三个路由名指向同一个 adapter)
ctx.llm.registerAdapter(['opus', 'sonnet', 'claude-code'], piAiAdapter)
```

当一个请求指定 provider 为 `opus`，registry 找到注册了 `opus` 的 adapter，调用其 `stream()` 方法。

### 7.2.2 llm/stream waterfall

每个模型调用都会经过 `llm/stream` waterfall：

```typescript
'llm/stream'(this: LlmRuntime, options: GenerateOptions,
  next: () => AsyncIterable<StreamChunk>): AsyncIterable<StreamChunk>
```

这个事件是围绕实际 `adapter.stream()` 的洋葱中间件。它的典型用法：

- **llm-retry**：在 `next()` 返回的流中检测错误 chunk，透明重试。
- **llm-replay**（测试支持）：绕过 adapter，从录制文件重放流——这是无网络请求的测试。
- **session-title**：观察流，提取模型输出的第一段内容作为 session 标题。

监听器调用 `next()` 时分发到下一个监听器或最终的 adapter stream。监听器可以提前返回自己的 AsyncIterable 来短路调用。

### 7.2.3 BlockAssembler

原始 SSE stream 产出 `StreamChunk`（连续消息片段）。`BlockAssembler` 将这些 chunk 流式装配为完整的 `Block`（text、tool-call、reasoning 等）：

```typescript
const assembler = new BlockAssembler()
for await (const chunk of stream) {
  assembler.push(chunk)
}
const blocks = assembler.blocks()  // ContentBlock[]
const finish = assembler.finish    // { kind: 'completed' | 'max-tokens' | 'error' | 'aborted' }
```

`BlockAssembler` 在 `packages/llm/llm/src/assembler.ts` 中实现。它维护内部状态来跟踪正在进行的 tool-call 块，在收到 `tool-call-begin` 时创建新块，在收到 `tool-call-end` 时关闭它并添加到 `blocks` 数组。

## 7.3 请求构建

在 `ReactLoopAgent.step()` 中，完整的模型请求通过 `buildRequest()` 构建：

1. 调用 `ctx.systemPrompt.assemble({ agent, scope })` 获取 PromptAssembly。
2. 调用 `renderPrompt(assembly)` 得到 system prompt 字符串。
3. 调用 `this.session.deriveMessages()` 获取模型历史消息。
4. 调用 `renderContextSnapshot(assembly)`，将得到的快照作为额外的 user/message 追加到消息列表。
5. 结合工具 schema，创建 `LlmCallConfig`。
6. 触发 `agent/request` waterfall——插件可以修改请求的任何字段（provider、model、maxTokens 等）。
7. 触发 `ctx.llm.stream(config)`，进入 adapter waterfall。

这个流程的每一步都开放给插件。特别是第 6 步 `agent/request` 是模型路由切换的位置——一个插件可以拦截请求，将 provider 从 `sonnet` 改为 `opus-4.5`，而主循环完全不需要知道路由逻辑。

## 思考题

1. `complete: true` 标记的 section 是什么设计意图？在什么场景下一个插件需要声明自己是"完整 prompt"？
2. `system-prompt/assemble` waterfall 如果返回的 assembly 中 sections 数量为 0，模型请求会怎么做？
3. 变量 provider 函数在 `assemble()` 时被调用，但如果一个变量的值在 prompt 渲染前发生了变化（如时间戳差了几毫秒），模型会看到哪个值？
4. `llm/stream` waterfall 可以替换整个流。如果用一个监听器把自己的屏幕录制文件作为假流注入，`agent-loop` 是否能感知到注册表已拦截？