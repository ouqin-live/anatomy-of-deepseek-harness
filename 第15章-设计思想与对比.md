# 第 15 章 设计思想与对比

## 15.1 五个设计决策

dsh 作为一个 Agent 运行时框架，最重要的五个设计决策决定了它与竞品的区别。

### 15.1.1 事件溯源（Event Sourcing → Session Log）

用不可变追加日志取代可变消息数组。这个决策是 dsh 的基础。

后果：所有状态重建不依赖运行时缓存。fork、resume、replay 共享同一组函数（`foldSurface` + `deriveMessages`）。模型可见的内容不是"被记录的"，而是"从日志中算出来的"。

代价：简单的消息历史查询（"最后三条用户消息是什么"）需要遍历整个事件列表并投影，而不是直接读数组。这是缓存的必要性所在——`deriveMessages()` 的增量缓存就是这么来的。

### 15.1.2 通过 Cordis 实现"插件即一切"

没有"核心框架"和"外围插件"的二元区分。AgentLoop 本身是一个插件，LLM Runtime 是一个插件，System Prompt 是一个插件。

后果：每个功能域（工具、文件系统、shell、存储、压缩）都通过 Cordis Service 暴露，通过 Typed Event 通信。框架不需要预定义扩展点——每个事件都是一个扩展点。受 Event Sourcing 中"append-only"概念的启发，dsh 的事件定义在编译期通过 declaration merging 扩展，没有运行时事件注册中心。

代价：超过 50 个包的包数量意味着分支认知负担。新开发者需要理解 Cordis 的 Context/Service/Event/Effect 四个概念才能开始编写插件。

### 15.1.3 能力接缝（Capability Seam）模式

每个与环境交互的能力（FS、Shell、Subagent）都用三部分定义：Service Definition（接口）、Provider（实现）、Consumer（消费）。策略层通过监听 Service 的事件插入。

后果：策略不依赖工具实现。`fs/write-intent` waterfall 不管是 `write_file` 工具还是 `edit_file` 工具都会触发。Policy 不管工具名是什么，只管文件路径。

代价：一个简单的"写入文件"操作可能经过 5 个事件的链条。调试时需要追踪事件链条。

### 15.1.4 Model-Visible Means Logged

模型请求中出现的每一个消息，在发送请求之前就已经被写入 session 日志。

后果：fork 和 resume 不需要在 fork/resume 时刻捕获"当时的消息快照"。从 fork 点之后的日志重新投影的消息列表即 fork 时模型看到的内容。

代价：启用了这个属性后，需要在 turn 的 log append 操作和模型请求发送操作之间仔细排序——在 `turn()` 代码中，`session.append('user/message')` 必须出现在 `step()` 之前。

### 15.1.5 没有特权内核（No Privileged Kernel）

AgentLoop 只是一个实现了 `Agent` 接口的插件。框架没有"核心"——启动加载器决定哪些插件被加载，加载的插件之间通过事件通信。

后果：可以替换 AgentLoop（`schedule` 插件提供了不同的执行模型），可以替换 Compaction Engine，可以替换 Storage。任何部分都不在"不可触及的核心"中。

代价：从产品角度来看，没有默认入口。用户必须知道哪个 Bundle 是做什么的，才能正确启动 dsh。

## 15.2 与 Claude Code 的比较

dsh 和 Anthropic 的 Claude Code（claude-code CLI）走的是不同的设计路线。Claude Code 的产品设计模式是"高品质、封闭式、一键即用"，dsh 的设计模式是"插件化、开放式、可组合"。

在 session log 的架构路线上，双方走了不同但有相互启发意义的道路。Claude Code 在著名的"关于 RAL 的争议"中长期使用的是"三条原则的纯 append-only event log 架构"：`SessionEvent` 不可变追加日志 + 表面层投影 + "model-visible means logged"。这一架构与高版本的 dsh 完全一致。Claude Code 在其第三方向开发者提供的 agent SDK（`claude-agent-sdk`）中保留了 event log 的设计，但核心驱动循环（agent-loop）做了不同的取舍。

在可扩展性层面，Claude Code 对内建 agent 完全封闭——你不能替换它的 AgentLoop，不能替换它的 System Prompt 组装逻辑。它通过 Hooks 系统（`packages/hooks` 的子包）开放有限的扩展点。dsh 通过 Cordis 的事件系统开放所有扩展点，但代价是需要用户对框架有更深的理解。

## 15.3 与 LangChain 的比较

LangChain 是 Agent 框架的另一种做法。LangChain 以 Chain（链）和 Runnable 为基本抽象，强调"构建块"的堆叠。

dsh 不提供 Chain 抽象。dsh 的抽象是 Session（会话日志）、Agent（执行循环）、Tool（工具定义）。开发者不堆叠 runnable，而是编写插件在事件边界上注册监听器。

两者的根本区别：LangChain 要求你描述 "模型如何被调用"（通过 Chain/LangGraph 的边和节点），dsh 要求你描述 "系统应该如何被组装"（通过 Bundle/Profile 的插件集合）。

## 15.4 附件设计哲学

dsh 的 Attribution 系统体现了它的设计哲学。Attribution（`packages/llm/llm/src/attribution.ts`）在每个模型请求中嵌入请求标识信息，使得 provider 能返回对应的诊断数据。这是一个小功能，但其设计体现了三个原则：

1. **可追溯性是内置属性**：不是作为可选的日志增强，而是作为请求的一部分强制注入。每个请求都可以追溯到生成它的 session/turn/step。
2. **不依赖外部服务**：Attribution 不依赖日志聚合、数据库查询。数据在请求本身之中。
3. **低侵入**：Attribution 通过 pi-ai 在请求的 header/body 层面添加数据，AgentLoop 完全不需要知道 Attribution 的存在。

## 15.5 版本策略

dsh 的版本策略遵循 语义化版本 (SemVer)。零重大版本 (`0.x`) 阶段意味着 API 可以随时向后不兼容变更。从 commit `141eb6f`（2026 年 8 月）的状态看，API 趋于稳定，但部分插件（如 pi-ai 和 Agent Communication Protocol）的向后兼容尚未锁定。

本书锁定的 commit 版本不代表 dsh 的最终状态。建议读者在阅读完本书后，checkout 最新的 master 分支，对比差异——变化的往往是稳定性和性能，不变的是事件溯源、能力接缝、没有特权内核这三个核心设计原则。

## 思考题

1. "Model-visible means logged" 在 fork 场景中有一个推论：fork 时不需要捕获消息快照。这个推论的前提是什么？如果 fork 后原始 session 被 compaction 修改了 surface，fork 后创建的 session 会受到影响吗？
2. dsh 选择了 "插件即一切" 的极端路线。如果 AgentLoop 是一个插件，那么 "一个没有 AgentLoop 的 dsh 实例" 是什么？它还能运行吗？
3. 与 LangChain 相比，dsh 的 "不在 Chain 中描述模型调用，而在事件边界注册监听器" 的设计对 debug 体验有什么影响？跟踪一个工具调用的完整路径是更难还是更容易？
4. Claude Code 选择了封闭内核 + 有限 Hooks，dsh 选择了开放内核 + 事件插件。在什么场景下，封闭内核比开放内核更合适？