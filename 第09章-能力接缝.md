# 第 9 章 能力接缝（Capability Seam：FS / Shell / Sandbox）

## 9.1 什么是能力接缝

"能力接缝"（capability seam）是 dsh 的一个设计模式，也是一个目录名（`packages/fs`、`packages/terminal`、`packages/subagent` 下的包都属于能力接缝的实现）。它由三层构成：

1. **Service Definition**（接口层）：一个 Cordis Service，定义能力的事件和主要 API。
2. **Provider**（实现层）：一个或多个插件，实现该 Service 的接口并注册到 Service 的注册表。
3. **Consumer**（消费层）：一个或多个插件，通过 Service 使用该能力。

三层之间通过 Typed Event 通信。Service 定义层声明事件（如 `fs/write-intent`），Provider 和 Consumer 都能监听这些事件。这种设计使"策略"（policy）可以独立于实现和消费。

## 9.2 文件系统接缝（FS Seam）

FS Seam 是能力接缝中最典型的一个。它包含这些包：

- **fs**（`packages/fs/fs`）：Service 定义。声明 `fs/write-intent` 和 `fs/edit-intent` 两个 waterfall 事件，以及 `fs/observed` emit 事件。
- **tool-fs**（`packages/fs/tool-fs`）：工具层，将文件读写操作封装为模型可调用的工具。
- **fs-observation-policy**（`packages/fs/fs-observation-policy`）：策略层，审计和记录文件访问。

事件定义：

```typescript
// packages/fs/fs/src/index.ts，第 55-80 行
'fs/write-intent'(exec: FileWriteIntent, next: () => Promise<WriteDecision>): Promise<WriteDecision>
'fs/edit-intent'(exec: FileEditIntent, next: () => Promise<EditDecision>): Promise<EditDecision>
'fs/observed'(exec: FileObserved): void
```

`write-intent` 和 `edit-intent` 是 waterfall——每个监听器可以批准或拒绝一次写入。`fs-observation-policy` 在这里注册，确保所有文件操作的意图都通过策略审查。`observed` 是 emit——在工具执行后通知审计系统。

一个 key 实现是 `str-replace-editor`（`packages/fs/tool-str-replace-editor`）：它实现了一个基于字符串替换的编辑工具。与 `tool-fs` 的直接读写不同，`str-replace-editor` 提供 `edit_file` 工具——模型给出文件中要替换的原文和新文本，工具定位并执行替换。它也触发 `fs/write-intent` 和 `fs/edit-intent`，所有策略对两种工具一致。

FS Seam 的整个设计确保模型不会绕过策略直接操作文件。无论模型使用 `write_file`、`read_file`、`edit_file` 中的哪个工具，最终都到达同一个事件边界上的同一个策略。

## 9.3 Shell 接缝（Terminal Seam）

Shell Seam 处理 bash 命令执行。它的实现包是 `packages/terminal/terminal-bash`。

Shell 接缝的核心是一个 terminal-bash 的 Service 接口，它不会自动创建一个可交互的 bash 进程（因为不是所有环境都支持，比如纯浏览器的 IDE 插件不需要 Shell）。当插件请求执行 shell 命令时，`terminal-bash` 判定当前环境是否可用。如果不可用，抛错误；如果可用，创建一个子进程。

Shell 接缝的事件边界包括命令执行前后的钩子，以及输出内容的审计。事件通过 `terminal-bash` 包内部定义但也可被策略插件拦截。

Shell 接缝与 FS Seam 的一个重要区别：Shell 接缝目前是单一实现，不存在多个 provider 竞争的注册表。Cordis 的 `inject` 机制确保只有一个 shell executor 被加载。

## 9.4 沙箱接缝（Sandbox Seam）

Sandbox Seam 通过 `packages/sandbox/sandbox-policy` 实现。它是一个围绕模型输出检测的策略插件，通常配合 Shell 接缝使用。

当模型输出一个命令时，`agent-loop` 的 `executeToolCalls()` 调用 `ctx.sandbox` 服务检查命令是否在 allowed list 中。Sandbox 可以配置为：

- **none**：不检查，所有命令允许。
- **allow-list**：只有命令名称在 allow list 内的才能执行。
- **block-list**：命令名称在 block list 内的拒绝执行。

沙箱判断通过 `tools/pre-execute` 上的监听器实现，作为整体工具执行管线的一部分。

## 9.5 审批接缝（Approval Seam）

Approval Seam 由 `packages/interaction/user-approval` 定义。它暴露 `approval/request` waterfall 事件：

```typescript
'approval/request'(exec: ApprovalRequest,
  next: () => Promise<ApprovalDecision>): Promise<ApprovalDecision>
```

当一个工具从 `tools/pre-execute` 获取到 `{ kind: 'ask' }` 决策时，`user-approval` 的预执行监听器不会立即批准或拒绝——它会触发 `approval/request` waterfall。该 waterfall 由 UI 插件（如 Web UI 或 terminal UI）监听，展示一个审批界面给用户。

在 headless 模式中，`approval/request` 可能由 `acp`（Agent Communication Protocol）监听——一个远程客户端通过 protocol 接收审批请求。

审批决议有三种结果：

- `approved`：批准执行。
- `rejected`：拒绝。
- `cancelled`：用户取消（不等同于拒绝）。

## 9.6 接缝之间的交互

各接缝不是孤立的。一个文件写入操作会穿过多个接缝边界：

1. 模型输出 `write_file` 工具调用。
2. `tools/pre-execute`：sandbox 策略检查工具名称。
3. `tools/execute`：timeout 策略包裹执行。
4. **FS Seam** `fs/write-intent`：文件写入策略审核写入目标路径。
5. 工具实际写入文件的 operate 函数。
6. **FS Seam** `fs/observed`：审计系统记录操作。
7. `tools/post-execute`：hook 系统处理结果。
8. `tools/result`：emit 通知所有观察者。

工具执行管线的五个阶段和各个能力接缝的事件在这里交汇。一个插件可以同时监听多个接缝、在多个阶段注入逻辑。这是"能力接缝"设计的核心收益——决策点在管线中分配，插件在需要的位置注册监听器，不需要修改管线本身。

## 思考题

1. FS Seam 的写意图事件在 `tools/pre-execute` 之前还是之后触发？这对策略的执行顺序有什么影响？
2. Shell 接缝没有多 provider 注册表，为什么 FS Seam 和 Subagent Seam 需要？是什么设计因素决定了这种差异？
3. 审批接缝的 `approval/request` waterfall 如果在 headless 模式中没有监听器响应，会发生什么？
4. 接缝之间的交互中，如果一个事件的监听器返回了拒绝决策，另一个接缝的已运行步骤如何回滚？