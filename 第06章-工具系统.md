# 第 6 章 工具系统（Tools：注册表与执行管线）

## 6.1 工具的声明与注册

在 dsh 中，一个工具由 `ToolDefinition` 接口定义：

```typescript
// packages/core/tools/src/index.ts，第 222-247 行
export interface ToolDefinition extends ToolSchema {
  readonly output: ToolOutputDefinition
  execute(args: unknown, exec: ToolRunContext): Promise<unknown>
  finalizeContent?(exec: Readonly<ToolExecution>,
    result: Readonly<ToolExecutionResult>): ContentBlock[] | undefined
}
```

它继承自 `ToolSchema`——name、description、parameters（JSON Schema）这三项是模型看到的内容。新增的三个字段是给运行时用的：

- **output**：声明工具返回值的 JSON Schema（不是给模型看的，是给检查用的）。
- **execute**：工具执行函数。接收已解析的参数 `args` 和执行上下文 `exec`（包含 `signal` AbortSignal、`agent` 引用等）。
- **finalizeContent**：可选的同步后处理钩子。在工具执行完毕后、将结果写入 session 日志之前调用，可替换模型看到的工具结果 content（不影响持久化的原始值）。`spill-policy` 插件就是通过它实现的——当工具返回内容超出上下文预算时，把完整内容替换为摘要 + 定位符。

注册通过 `ctx.tools.register(toolDef)` 完成：

```typescript
const disposer = ctx.tools.register({
  name: 'read_file',
  description: 'Read a file from the filesystem',
  parameters: { type: 'object', properties: { path: { type: 'string' } } },
  output: { schema: { type: 'string' }, render: (args, value) => [{ type: 'text', text: value }] },
  execute: async (args, exec) => {
    const content = await fs.readFile(args.path, 'utf-8')
    return content
  },
})
```

`register()` 内部通过 `ctx.effect()` 注册——当插件卸载时自动撤销。这保证了 HMR 过程中旧工具被移除、新工具被注册。

工具注册在 scope 级别可见。scope 层的 `ToolLayer` 维护 `NamedEntries<ToolDefinition>`。同名的 scoped 工具 shadow 全局工具——这是子代理实现局部工具可见性的基础（一个子代理持有自己的 agent-scoped Context，在其上注册的工具只在子代理的 prompt 中可见）。

## 6.2 执行管线的五段流水线

`ReactLoopAgent` 在 `step()` 中检测到 `tool-call` Block 后，调用 `executeToolCalls()`，进入工具执行管线。完整的管线分为五个阶段：

```
tool/call 事件（日志） → tools/pre-execute（waterfall）
                      → guards（单调守卫）
                      → tools/execute（waterfall/around‑dispatch）
                      → tools/post-execute（waterfall）
                      → tool/result 事件（日志）
```

### 阶段 1：tools/pre-execute（waterfall）

第一道决策关卡。`exec` 对象携带工具名、解析后的参数、agent 引用和 AbortSignal。每个注册的监听器可以：

- 返回 `{ kind: 'allow' }`——批准执行。
- 返回 `{ kind: 'deny', reason }`——拒绝，附原因。
- 返回 `{ kind: 'ask' }`——请求用户确认（触发 `approval/request` waterfall）。
- 不调用 `next()`——短路，后面的监听器不再执行。

这个阶段是权限策略的核心位置。`hooks-claude-code`、`hooks-codex`、`tool-jobs` 等插件在此注册。

### 阶段 2：guards（单调守卫）

预执行 waterfall 通过后，进入**单调守卫**（monotonic guard）阶段。守卫不能批准一个被拒绝的调用，只能拒绝或弃权（返回 undefined）。因为守卫返回拒绝理由（`string | undefined`），不设置审批结果，所以守卫的顺序不影响最终结果——任何一个守卫拒绝就拒绝：

```typescript
// packages/core/tools/src/index.ts，第 704-711 行
export type ToolGuard = (execution: Readonly<ToolExecution>) => string | undefined
```

守卫由 `ctx.tools.registerGuard(guard)` 注册。如 `permission-presets` 插件会注册身份保护守卫，防止模型暴露 API key 等敏感信息。

### 阶段 3：tools/execute（waterfall/around-dispatch）

执行阶段。`next()` 调用工具注册的 `execute()` 函数。这个阶段通常由超时策略和重试策略包裹。`timeout-policy` 插件在这里注册——它用 `AbortSignal.timeout()` 包裹原始信号，在超时后取消执行。

`tools/execute` 是一个 around-dispatch waterfall：监听器可以包装 `next()`，设置新信号，等待结果。`exec.signal` 在此阶段可能被替换（但注册表会在执行 body 前重新融合原始 caller signal，所以替换不能脱离调用者的取消）。

### 阶段 4：tools/post-execute（waterfall）

执行结束后（不论成功或失败），进入后处理阶段。监听器可以：

- 接受原始结果（`{ kind: 'accept' }`）。
- 替换结果内容（`{ kind: 'replace', content }`）。
- 拒绝结果（`{ kind: 'block', reason }`）。
- 注入额外上下文（`{ kind: 'accept', additionalContexts }`）。

这是 hook 系统的工作位置。在 Claude Code 的模式中，`hooks-claude-code` 在此阶段根据结果改写输出。

### 阶段 5：finalizeContent 与 tools/result（emit）

passing post-execute 后，注册表调用 `finalizeContent`（工具定义的可选同步回调），然后冻结结果，触发 `tools/result` 的 emit 事件：

```typescript
'tools/result'(this: Scoped<ToolRuntime>,
  exec: Readonly<ToolExecution>, result: Readonly<ToolExecutionResult>): undefined
```

`tools/result` 是 emit，意味着监听器收到的是不可变（deep-frozen）的可读快照。监听器不能拒绝或修改结果——此时结果已经写在日志里了。

最后，`tool/result` 事件被 append 到 session 日志。

## 6.3 工具结果的上下文注射

执行管线最后一步有一个细节：如果 post-execute 返回了 `additionalContexts`，这些内容会在工具结果 append 之后、step 继续之前，作为 `user/message` 事件被注入 session 日志。这不是模型可见的"工具结果"，而是一个额外消息，用于提供模型需要知道的额外上下文但不属于工具返回值的语义。

例如，spill-policy 在 `tools/post-execute` 中检测到工具返回内容太大，无法放进上下文预算。它的策略是：将完整内容写入文件，将文件摘要 + 定位符放入模型可见的 content 中，同时通过 post-execute 的 `additionalContexts` 注入一条包含文件路径的用户消息。模型在下一个 step 中看到这条消息，知道还有额外内容可在需要时读取。

## 6.4 工具的执行模式

默认情况下，工具在一个 turn 内按模型返回的顺序串行执行。但 dsh 提供了 `maxParallelToolCalls` 配置来启用并行执行。并行模式下，注册表会维护一个滚动池（rolling pool），按模型返回的工具调用顺序启动，但允许并行执行不冲突的工具。

并行执行的调度由 `ToolExecutionScheduler` 类负责。调度分为三个类别：

- **post-result**：串行处理，结果直接附加到状态，等待所有前置完成。
- **final-result**：在当前批次的上下文注入后再返回最终消息，用于需要在 turn 结束前对结果做统一处理的工具。

调度器在司机的 `executeToolCalls()` 中运行，处理后返回的 `concluded` 布尔值决定 `step()` 返回 `null`（继续下一个 step）或 `{ kind: 'completed' }`（关闭 turn）。

## 6.5 工具视图与可见性

不是所有注册的工具都在模型 prompt 中可见。`scope` 系统负责控制每个 agent 看到哪些工具：

- 全局注册的工具对所有 agent 可见。
- agent-scoped 注册的工具只对该 agent 可见。
- `ToolRestriction`（`allow`/`deny` 列表）可以在 scope 级别压缩工具集。

工具视图在 System Prompt 的 `assemble()` 过程中被收集，通过 `systemPrompt.tools()` 注册的 provider 函数返回 `ToolProviderResult`。视图根据 toolOrder 排序后，作为 LLM request 中的 `tools` 参数发送。

## 思考题

1. `tools/post-execute` 返回 `{ kind: 'replace', content }` 后，`finalizeContent` 还会被调用吗？替换后的 content 是否遍历了 finalizeContent？
2. 并行执行工具时，如果一个工具超时，已成功但未写入日志的工具结果会被回滚吗？
3. 单调守卫（monotonic guard）为什么设计为不可撤销的拒绝？如果需要在审批流程中撤销决策，应该在哪一个阶段实现？
4. `tools/result` 作为 emit 事件，如果监听器抛出异常的行为是怎样的？注册表是否会阻止后续监听器执行？