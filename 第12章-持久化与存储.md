# 第 12 章 持久化与存储（Store / Fork / Resume）

## 12.1 存储架构

dsh 的存储系统将 session 日志的持久化与 session ID 的存储域管理分离为两个互不依赖的 Cordis Service。

- **Session Store**（`packages/core/session`）：定义 `SessionStore` 抽象 Service，提供 `prepare → enter → announce` 三段式原子创建流程和 `resume` 方法。不负责具体的序列化。
- **Storage Domain**（`packages/storage/storage-domain`）：管理存储域（storage domain），决定 session 数据写到哪里（本地文件系统、IndexedDB、云存储等）。
- **Storage FS**（`packages/storage/storage-fs`）：默认的本地文件系统实现，将 session 日志写入 JSON 文件。

## 12.2 Session Store 的创建

`SessionStore` 是 `packages/core/session/src/index.ts` 中的 Cordis Service。创建 session 的流程：

```typescript
// index.ts 中的 prepare → enter → announce
const prepared = sessionStore.prepare(sessionHeader, seedEvents)
// prepared.session 此时存在但尚未注册
// ... 可选的 setup 步骤 ...
sessionStore.enter(prepared.session)
// session 进入 registry，但未发布创建通知
sessionStore.announce(prepared.session)
// session/created 事件触发，所有监听器收到通知
```

这三段式的设计目的是：在 session 注册到外部可见的 registry 之前，setup 代码可以对 session 进行修改（如注入初始消息、设置工具可见性 etc.）。一旦 `announce`，session 就稳定了，监听 `session/created` 的插件可以安全地获取 session 快照并开始工作。

## 12.3 持久化

持久化由 `session-persistence` 插件实现。它监听 `session/event` 事件——每当 session 日志追加新事件，persistence 读取新事件并写入存储域。

```typescript
// session-persistence 中
ctx.on('session/event', async (payload) => {
  const { session, events } = payload
  await storageDomain.append(session.id, events)
})
```

`session/flush` 是 parallel 事件——persistence 和 telemetry 同时收到通知，并行持久化。flush 在 turn 边界处由 `agent-loop` 触发：

```typescript
// agent-loop 在 turn/end 后调用
ctx.parallel('session/flush', { session })
```

## 12.4 存储域（Storage Domain）

`storage-domain` Service 抽象了"数据存到哪里"的逻辑：

- **`storage-fs`**：写入本地文件系统的 `.dsh/sessions/` 目录。每个 session 一个 `.jsonl` 文件，事件逐行追加。
- **`storage-indexdb`**：用于浏览器环境（如 Web UI 的运行在浏览器中的 IDE 插件），写入 IndexedDB。
- 用户也可以编写自定义存储域（如写入 S3 或 PostgreSQL）。

存储域的 key 是 `SessionId`（类型安全的 branded string）。每个存储域实现 `append(id, events)`、`read(id)`、`delete(id)` 等方法。

## 12.5 Resume（恢复）

当一个 session 被持久化后，可以在后续启动中恢复：

```typescript
const session = await sessionStore.resume(sessionId)
```

`resume()` 从存储域读取事件日志，重建 Session 实例，重建 SurfaceManager，重建消息缓存。恢复后的 session 与从未停止的 session 在语义上等价——`deriveMessages()` 返回相同的消息列表。

Resume 不重建 Agent——session 恢复是存储层的操作，不涉及 Agent 主循环。Agent 的创建在 session 恢复后由 `ctx.agents.create(session, options)` 完成。

## 12.6 Fork（分支）

Fork 基于 session 的不可变事件日志：

```typescript
const fork = await sessionStore.fork(sessionId, { atSequence: lastSeq })
```

`fork()` 读取指定 session 的事件日志到 `atSequence` 位置，将该位置的事件作为新 session 的种子，创建一个新 session ID。新 session 拥有旧 session 在该时间点的所有消息历史，但后续事件不会影响旧 session。

Fork 的应用场景：

- **探索性对话**：用户想从某个历史时刻重新开始对话，尝试不同的方向。
- **可比较的决策路径**：同一个问题，不同参数的不同响应。
- **测试**：从生产环境的 session 截取特定 turn 序列，创建测试 session。

Forked session 与父 session 完全独立。写入 forked session 的事件不会出现在父 session 中，反之亦然。唯一共享的是 fork 点之前的原始事件（从存储角度看，技术实现上可能通过文件系统硬链接或共享 blob 来避免数据复制）。

## 12.7 Session 的生命周期事件

Session 的声明周期由以下 Cordis 事件管理：

- `session/created`：session 刚刚被创建并 announce。
- `session/event`：session 日志中追加了新事件（emit，不阻塞）。
- `session/flush`：要求持久化插件刷入存储（parallel，等待持久化完成）。
- `session/disposed`：session 被销毁。

Agent 生命周期与 session 生命周期不同步。Agent 创建时监听 `agent/created`，Agent 销毁时监听 `agent/disposed`。一个 session 可以在多个 agent 的生命周期中保持活跃。但持有 session 的 agent 被销毁后，session 本身不会自动销毁——这由拥有 session 的管理策略决定。

## 12.8 投影缓存

`session-projection-cache` 是 session 扩展包中的一个重要性能设施。当多次对同一个 session 调用 `deriveMessages()`，每次都重新投影全部事件是浪费的。投影缓存维护每个 session 的缓存行：

- 缓存以 `sessionId` + `(lastSeq, generation)` 为键。
- 每次 `session/event` 到来时，缓存行失效。
- 缓存行重建时，从存储域读取增量事件，仅投影上次缓存后新增的事件。

## 思考题

1. `prepare → enter → announce` 三段式设计中，如果在 `enter` 和 `announce` 之间插入了一个异步 setup 操作，而 setup 抛出了异常，应该如何处理？session 是否应该被回滚？
2. 存储域（storage-domain）和 session store 之间的职责划分是怎样的？session store 是否知道数据是存在文件系统还是 IndexedDB？
3. Fork 的 `atSequence` 参数如果等于当前 session 的最新事件序号，行为是什么？和正常 resume 有什么不同？
4. Session 创建后但从未 flush 的数据，在进程异常退出后是否会丢失？`session/flush` 事件在什么时机触发？