# 第 3 章 组合树（Profile / Bundle / Patch）

## 3.1 从 main 函数到声明式组合

大多数程序有一个 main 函数，依次初始化各个组件。dsh 没有 main 函数。启动时，它读取一个 YAML 配置文件，然后实例化一个 Cordis App，YAML 中的 `plugins` 列表定义了要加载哪些插件。

这是声明式组合：你不需要写代码决定如何组装系统，而是写一个配置文件描述系统中应该有哪些组件。Cordis 加载器根据这个描述自动解析依赖、创建 Context 实例、安装插件。

三个关键概念：**Profile** 定义哪些插件可用；**Bundle** 定义哪些插件启用；**Patch** 在运行时生效的特殊配置层。

## 3.2 Profile

Profile 是 dsh 的插件目录。它定义了哪些 package 可以出现在组合树中。默认 profile 通常在 `bundles` 目录下以 `ds-profile-` 前缀命名。Web 部署的 profile 和 headless 部署的 profile 可能不同。

一个 Profile 大致是这样的结构：

```yaml
# bundles/@deepseek-ai/ds-profile-standard — 标准 profile
plugins:
  session: $include ../../packages/core/session
  agent: $include ../../packages/core/agent
  agent-loop: $include ../../packages/core/agent-loop
  tools: $include ../../packages/core/tools
  system-prompt: $include ../../packages/core/system-prompt
  llm: $include ../../packages/llm/llm
  settings: $include ../../packages/settings/settings
  # 更多插件...
```

每个 `$include` 指向一个 npm package。加载器找到这个 package 后，读取其 `package.json` 中的 `main` 或 `exports` 字段，require 默认导出——它应该是一个 Cordis 插件（函数或 Service 类）。

Profile 只声明"有哪些包"，不设置它们的 `config`。激活阶段由 Bundle 完成。

## 3.3 Bundle

Bundle 是一个可发布的插件组合。它包括三部分：

```yaml
# bundles/@deepseek-ai/ds-agent-default — 默认 Agent bundle
plugins:
  # 引用 profile 中的插件
  session: {}
  agent: {}
  agent-loop: {}
  # 设置配置
  system-prompt:
    config:
      includeHarnessIdentity: true
      persona: |
        You are a helpful AI assistant...

meta:
  title: DeepSeek Agent - Default
  description: Default agent configuration
```

Bundle 中的 `plugins` 字段引用 Profile 中声明的插件名称（如 `session`、`agent`）。它可以选择性地为每个插件设置 `config`。

Bundle 可以互斥。产品环境中，用户只能用一个 bundle，因为它定义了 Agent 的完整行为。但加载器可以同时加载多个 Profile——profile 只是注册了插件类，不激活它们。

Bundle 的 load 流程：

1. 加载器读取 bundle 的 YAML 文件。
2. 对每个 `plugins` 中的条目，在 Profile 中查找同名的插件。
3. 解析 `config` 字段（可能包含 `!!js` 表达式节点，如 `!!js/function` 或 `!!js/regex`）。
4. 创建 Cordis App，实例化每个插件，按 `inject` 顺序启动。
5. App 的 `start()` 成功后，通过 `ctx.agents.setFactory()` 将 AgentLoop 绑定到 Agent 工厂上。

## 3.4 Patch

Patch 是运行时生效的覆盖层。当用户需要在已运行的 dsh 实例中修改配置而不重启时，Patch 提供热更新的能力：

```yaml
# 运行时补丁：打开推理预算
patch:
  turned: false          # 补丁标识
  match:                 # 匹配条件
    agentId: main
  overrides:
    system-prompt:
      config:
        persona: "You are a coding expert. Use extensive reasoning."
    llm:
      config:
        maxTokens: 16384
```

Patch 在运行时通过 Cordis 的 `settings/updated` 事件生效。当用户通过 Web UI 或 API 应用一个 patch 时，`settings` Service 的 `update()` 方法被调用，它重新解析配置文件中的 patch 部分，找到匹配的 agent，然后调用受影响 Service 的 `reconfigure()` 方法。

Patch 不是持久化的——重启后丢失。它是"运行时临时覆盖"的语义。

## 3.5 加载器（Loader）

加载器是启动入口的实现。默认加载器在 `packages/boot/loader` 中。它的主要职责：

1. **解析入口配置**。从命令行参数或环境变量读取 bundle YAML 路径。
2. **解析表达式节点**。YAML 中的 `!!js` 标签（如 `!!js/function`、`!!js/regex`）在运行时求值。例如：

```yaml
hooks-claude-code:
  config:
    systemPromptSection:
      name: claude-code:hooks
      order: 200
      text: !!js/function |
        function(context) {
          return `Current date: ${new Date().toISOString().slice(0, 10)}`
        }
```

`!!js/function` 将字符串解析成一个 JavaScript 函数。加载器调用 `eval()`（在沙箱化的 VM 上下文中）将 YAML 文本变成运行时对象。

3. **依赖解析**。每个插件的 `inject` 数组列出了它需要的 Service。加载器将所有插件排序，使依赖在 `apply()` 之前就绪。循环依赖在启动时抛错。

4. **Disabled 字段**。插件可以设置 `disabled: true` 或 `disabled: !!js/expression`（在加载器上下文中求值）。例如根据环境变量禁用：

```yaml
llm-replay:
  $include: ...
  disabled: !!js/expression process.env.NODE_ENV === 'production'
```

## 3.6 组合树的运行时视图

启动完成后，Cordis App 内的组合树可以通过 Web UI 的 `/inspect` 端点查看。它显示：

- 所有加载的 Service 实例，按包名分组。
- 每个 Service 的 `inject` 依赖关系图。
- 每个 Service 的 config（可在 UI 中查看当前值）。
- 每个 Service 注册的事件监听器列表（事件名、注册来源、注册顺序）。

这个结构对调试很有帮助。如果一个插件没有按预期行为运行，首先检查它是否被加载、config 是否正确、它监听的事件是否被触发。

## 3.7 配置文件速查

每个 Service 有自己的 `Config` 类型，定义在 Service 类的 `static Config` 字段中（使用 schemastery schema 定义）。加载器读取 YAML config 后，会通过 schemastery 的 `validate()` 函数验证类型和默认值。验证失败时抛出错误并终止启动。

常规配置文件搜索路径（按优先级从高到低）：

1. 命令行 `--config` 指定的路径
2. `~/.dsh/settings.yaml`
3. 当前目录下的 `cordis.patch.yml`
4. bundle 内置的默认配置

高优先级的文件覆盖低优先级文件的同名键。合并不是简单的深合并——每个 Service 的 config 作为独立对象处理，不会跨 Service 合并。

## 思考题

1. Bundle 和 Profile 的分离带来什么好处？为什么不能合为一个概念？
2. `!!js/function` 表达式节点在加载器中被 `eval()` 执行，这对安全性有什么影响？dsh 如何缓解这个风险？
3. Patch 不持久化的设计意图是什么？如果用户需要一个持久化的配置覆盖，应该怎么做？
4. 如果一个插件在 `config` 中设置了 `disabled: true`，但另一个插件通过 `inject` 依赖了它，启动时会发生什么？