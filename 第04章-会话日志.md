# 第 4 章 会话日志（Session：Append-Only Event Log）

## 4.1 问题的起点

在大多数 Agent 框架中，历史消息是一个 `Message[]` 数组。每个 LLM 请求从数组尾部取出所有消息，新消息追加到尾部，compaction 从头部裁减。模型可见的内容就是这个数组的全部。它简单、直观，但有几个问题：

1. **回放（replay）依赖外部快照**。如果要把模型回到某个历史时刻，需要事先保存该时刻的消息快照。快照与原数组之间没有可验证的对应关系。
2. **分叉（fork）需要复制数组**。每条分叉路径持有独立的副本，副本之间没有共享的原始数据。
3. **工具调用、流式 chunk、错误这些非模型数据无处安放**。日志信息（调用哪个模型、消耗多少 token、哪步出错）只能捎带在消息结构中或者存在另一个存储里。
4. **compaction 破坏历史**。裁减掉的头部消息在原始上下文中不再可用——即使轮次编号或时间戳能追溯，正文已丢失。

dsh 的解答：不要用可变数组，用**不可变事件日志**。

## 4.2 `SessionEvent` 类型体系

每个 `Session` 维护一条 append-only 的 `SessionEvent[]` 数组。数组只增不减，不可原地修改。它的类型定义如下：

```typescript
// packages/core/session/src/types.ts，第 408-440 行
export type SessionEvent<T extends SessionEventType = SessionEventType> = {
  [K in SessionEventType]: {
    type: K
    /** Monotonic sequence number within the session. */
    seq: number
    /** Unix epoch milliseconds. */
    time: number
    data: SessionEventMap[K]
    ignorable?: true
  } & (K extends SurfaceEventType ? {
    sourceEventSeqs?: number[]
    /** How this event entered the surface; absent for non-surface events. */
    surfaceOp?: SurfaceOp
  } : object)
}[T]
```

这是 TypeScript 的分布式条件类型：`SessionEvent` 不是一个大对象，而是 `SessionEventMap` 中每个键产出一个单分支类型的联合。每个事件必有三个固定字段——`type`（事件种类）、`seq`（单调递增的序号）、`time`（毫秒级时间戳）——加上一个 `data` 数据体。后续 `K extends SurfaceEventType` 的条件分支只在特定事件类型上附加 `surfaceOp` 和 `sourceEventSeqs` 两个字段，编译器会检查 call site 是否传了正确的字段。

这个类型设计的两个要点：

- **真正的判别联合**：`switch (event.type)` 直接收窄 `event.data`，不需要 `as` 或类型守卫。这在处理事件的代码里每天被调用。
- **surface 元数据只在消息生产事件上存在**：事件分为两类——能派生出 LLM 消息的（用户消息、助手回复、工具结果）和不能的（轮次边界、流式 chunk、调用统计）。前者有 surface 字段，后者编译期不允许携带。

有哪些事件类型？`SessionEventMap` 接口列出了完整的词汇表：

```typescript
// types.ts，第 236-337 行
export interface SessionEventMap {
  'turn/start': { turn: number }
  'turn/end': { turn: number; reason: TurnEndReason }
  'step/start': { turn: number; step: number }
  'step/end': { turn: number; step: number }
  'user/message': UserMessage
  'assistant/chunk': { turn: number; step: number; chunk: StreamChunk }
  'assistant/message': { turn: number; step: number; message: AssistantMessage;
    usage?: TokenUsage; interrupted?: true }
  'tool/call': { turn: number; step: number; callId: CallId; name: string; arguments: string }
  'tool/result': { turn: number; step: number; message: ToolResultMessage;
    error?: { name: string; code: string }; meta?: JsonValue }
  'todo/write': { todos: TodoItem[] }
  'request/header': { header: EpochHeader; reason: RequestHeaderReason }
  'request/context': RequestContext
  'session/end-seed': Record<string, never>
}
```

这是一个可扩展的接口。Cordis 的模块合并（declaration merging）允许其他插件通过重新打开 `SessionEventMap` 来注入自己的事件类型，比如 `session-title` 包追加了 `session/title`。这些新类型在外部的 `SessionEventMap` 扩展里声明，编译器自动将它们加入 `SessionEvent` 联合。

事件的字段有以下含义：

- **msg vs assistant/chunk**：chunk 记录每个原始流片段，用于精确回放（tokens 级别重放）。assistant/message 是 `BlockAssembler` 组装完成后的消息内容。chunk 用于回溯精确的输出顺序和延迟；message 用于派生产生模型历史。
- **tool/call vs tool/result**：call 记录模型请求的工具调用 JSON 原文（未解析）；result 记录工具执行后的返回。两者用 `callId` 配对。
- **request/header 和 request/context**：每次 LLM 请求的配置（provider、model、system、tools 数组）以全量快照 append，后一条覆盖前一条。重放时从最新一条重建请求配置。

## 4.3 表面层（Surface）

不是所有事件都能进入模型视野。transform/start、transform/end、assistant/chunk、todo/write 这些事件只是辅助数据。只有三种事件类型会上"模型可见表面"（model-visible surface）：

```typescript
// types.ts，第 347-351 行
export type SurfaceEventType =
  | 'user/message'
  | 'assistant/message'
  | 'tool/result'
```

这三种事件附带一个 `surfaceOp` 字段，标记该事件如何进入表面：

```typescript
// types.ts，第 376-378 行
export type SurfaceOp =
  | 'append'
  | { op: 'replace'; start: number; end: number }
```

- **append**：追加到表面尾部——正常的消息写入路径。
- **replace**：替换表面上从 seq `start` 到 `end` 的已有节点——compaction 专用。被替换的节点在表面上消失，但仍在事件日志里持久存储（不会被删除），只是不再参与模型历史投影。

`surface.ts` 中的 `foldSurface` 函数完整地重放一次表面折叠：

```typescript
// surface.ts，第 387-395 行
export function foldSurface(events: readonly SessionEvent[]): SurfaceFoldResult {
  const state = createFoldState()
  const replacements: SurfaceFoldReplacement[] = []
  for (const [index, event] of events.entries()) {
    const replacement = applySurfaceEvent(state, event, index, events, 0)
    if (replacement !== undefined) replacements.push(replacement)
  }
  return { nodes: [...state.nodes], replacements }
}
```

这个函数按事件序号顺序扫描所有事件。遇到 append 事件，把序号 push 到表面节点列表尾部；遇到 replace 事件，从列表中移除 `start` 到 `end` 区间内的节点，压入新节点的序号。最终 `nodes` 数组给出模型可见的事件序列。

`SurfaceManager` 是一个增量版本（`surface.ts` 第 398-460 行），它在 `Session` 内部被持有。每次 `Session.append` 写入新事件后，`SurfaceManager` 自动折叠追加的事件。由于表面只保存事件序号而不是消息本体，多次折叠的代价是 O(N) 而不是 O(N × 消息体大小)。

## 4.4 deriveMessages()：从日志到模型历史

这是最关键的一行：用户消息、工具结果、助手回复——这些东西如何在 LLM 请求中组装成 `Message[]`？

```typescript
// index.ts，第 726-747 行
deriveMessages(): Message[] {
  const surface = this.surface
  const nodes = surface.nodes
  const generation = surface.replaceGeneration
  if (generation !== this.derivedGeneration) {
    this.derived = []
    this.derivedNodes = 0
    this.derivedGeneration = generation
  }
  for (const seq of nodes.slice(this.derivedNodes)) {
    const msg = this.deriveEventMessage(this.log[seq]!)
    if (msg) this.derived.push(msg)
  }
  this.derivedNodes = nodes.length
  return [...this.derived]
}
```

流程为：

1. 从 `SurfaceManager` 取当前表面节点（事件序号的列表）。如果发生过 replace（compaction），爬虫生成号变化，清空缓存重新开始。
2. 遍历上一次调用后新增的表面节点。对每个节点序号，从日志取出事件，调用 `deriveEventMessage` 将该事件投影为 `Message | null`。
3. 跳过投影为 `null` 的事件（例如 `assistant/message` 具有空内容，仅为记录 token 用量）。
4. 返回结果的**冻结副本**。每条 Message 对象是冻结的、可共享的——同一个 Message 同时出现在缓存、模型请求体和持久化日志里，全都没有副本。

`deriveEventMessage` 的投影规则很简单：

```typescript
// surface.ts，第 83-113 行
export function deriveEventMessage(event: SessionEvent): Message | null {
  switch (event.type) {
    case 'user/message':
      return event.data
    case 'assistant/message': {
      if (event.data.message.content.length === 0) return null
      return event.data.message
    }
    case 'tool/result':
      return event.data.message
    default:
      return null
  }
}
```

`user/message` 和 `tool/result` 直接返回事件本体携带的 Message（LMM API 认可的格式）。`assistant/message` 检查是否有实际 content 体，空消息（仅记录 token 用量）跳过。其他事件（chunk、边界、日志数据）全部投影为 `null`。

如果你有一个已经持久化的 session 事件日志，想在外部重建模型看到的消息列表，只需要两个函数就够了：`foldSurface(events).nodes` 得到表面节点排序，然后对每个 seq 调用 `deriveEventMessage(log[seq])` 得到 Message。

## 4.5 "Model-Visible Means Logged"

dsh 在代码注释中记录了一个运行时断言：

```typescript
// surface.ts，第 44-45 行
// The model-visible surface deliberately shadows replaced ranges, so it is the
// wrong source for a human transcript
```

这个断言在 `Session` 类的设计中贯彻始终。每次事件 append 的实际调用路径是：

1. 写入事件到 `events[]` 数组（日志层）。
2. `SurfaceManager.validateNext(event)` 验证 surfaceOp 的合法性。
3. 在 Cordis 事务完成时，`session/event` 事件被 dispatch（持久化插件会订阅它）。
4. 如果这些步骤中任何一步抛出错误，整个事务回滚，事件不会出现在日志中。

这意味着：模型请求中出现的每一个消息，在启动请求之前就已经被写入 session 日志。如果要查看一个 session 中模型看到的全部内容，你不需要捕获请求/响应，只需要把日志刷入 storage，回放事件序列、折叠表面、投影消息。结果与模型请求时看到的消息列表在语义上等价。

这也是 fork 和 resume 工作的基础：fork 点只是一个事件序号。从该序号之后的事件日志推导出的消息列表，与在该序号时刻发出的模型请求使用的消息列表完全相同。不需要在 fork 发生时保存一份"当时的消息快照"。

## 4.6 小结

事件日志是 dsh 中最基本的数据结构，也是它区别于大多数 Agent 框架的设计：

- **日志不可变且追加，在任何情况下都可重建模型历史**。不需要额外的快照或者副本。重放、分叉、恢复共用同一组函数。
- **表面层将日志投影为模型可见的顺序**。append 是正常的写入，replace 是 compaction——被替换的节点仍在日志中，只是不再投影。
- **类型系统强制执行表面合同**：只有消息生产事件才能携带 surfaceOp，日志事件在编译期区分两类用途。
- **"Model-visible means logged" 是一个不依赖运行时的属性**。你可以在 session log 导出后离线完整重建每次模型请求的输入。

## 思考题

1. `surfaceOp` 字段中 `'append'` 和 `{ op: 'replace', start, end }` 两种模式的运行时开销有什么不同？replace 是否需要遍历整个事件列表？
2. `deriveMessages()` 维护了一个缓存：`derived` 数组和 `derivedGeneration` 标记。如果一次 append 操作不触发 replace，缓存的命中逻辑是怎样的？
3. `foldSurface` 函数在重放事件列表时对事件序号的连续性做了断言（`event.seq !== expectedSeq` 会抛出异常）。如果从外部存储加载的事件列表中间有断开，会导致什么样的后果？
4. "Model-visible means logged" 的含义中，有一条反向自洽性质：模型请求的所有输入都已经在日志中。如果在请求过程中 LLM 抛出一个 HTTP 错误，日志中已经有哪些事件了？